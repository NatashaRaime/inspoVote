<%= form_with(model: voter, local: true) do |form| %>
  <% if voter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voter.errors.count, "error") %> prohibited this voter from being saved:</h2>

      <ul>
      <% voter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :voterId %>
    <%= form.number_field :voterId %>
  </div>

  <div class="field">
    <%= form.label :districtId %>
    <%= form.number_field :districtId %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :passwordHash %>
    <%= form.text_field :passwordHash %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
