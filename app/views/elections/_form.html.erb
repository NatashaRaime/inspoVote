<%= form_with(model: election, local: true) do |form| %>
  <% if election.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(election.errors.count, "error") %> prohibited this election from being saved:</h2>

      <ul>
      <% election.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :electionId %>
    <%= form.number_field :electionId %>
  </div>

  <div class="field">
    <%= form.label :districtId %>
    <%= form.number_field :districtId %>
  </div>

  <div class="field">
    <label> election date </label>
    <%= form.date_field :electionDate %>
  </div>
  
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <label><%= form.submit %></label>
  </div>
<% end %>
